import { Routes } from '@angular/router';
import { CurrentVisitComponent } from './current-visit.component';

export const CurrentVisitRoutes: Routes = [{
  path: '',
    component: CurrentVisitComponent
}];
